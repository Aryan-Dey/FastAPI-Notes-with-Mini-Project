# FastAPI-Notes-with-Mini-Project
FastAPI project showcasing backend fundamentals: Patient Management CRUD APIs with proper HTTP status codes, validation via Pydantic, query/path params, sorting, and robust error handling. Includes clean update logic with computed fields (BMI/verdict) recalculated, plus home/health endpoints and ML prediction API-ready structure.
